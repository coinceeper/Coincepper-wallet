name: my_flutter_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.48+52

environment:
  sdk: '>=3.2.3 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  
  # Navigation
  go_router: ^14.6.2
  screen_protector: ^1.4.2

  # UI Components
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.2
  provider: ^6.1.2
  cached_network_image: ^3.4.1
  flutter_svg: ^2.0.10+1
  fl_chart: ^0.69.0
  
  # Network & API
  dio: ^5.7.0
  dio_cookie_manager: ^3.1.2
  cookie_jar: ^4.0.8
  http: ^1.2.2
  connectivity_plus: ^6.0.5
  
  # Agent config at-rest (TSP1) — [scripts/encrypt_tsp_agent_config.py]
  cryptography: ^2.7.0

  # Storage & Database
  shared_preferences: ^2.3.2
  flutter_secure_storage: ^9.2.2
  sqflite: ^2.4.0
  
  # Device & Hardware
  device_info_plus: ^11.5.0
  package_info_plus: ^8.3.0
  permission_handler: ^12.0.1
  local_auth: ^2.3.0
  
  # Notifications
  flutter_local_notifications: ^19.3.0
  firebase_core: ^3.12.0
  firebase_messaging: ^15.2.0
  
  # QR & Scanner
  mobile_scanner: ^7.0.1
  qr_flutter: ^4.1.0
  
  # Utilities
  path: ^1.9.0
  path_provider: ^2.1.4
  crypto: ^3.0.5
  web3dart: ^2.7.3
  bip39: ^1.0.6
  bip32: ^2.0.0
  uuid: ^4.5.1
  pointycastle: ^3.9.1
  bech32: ^0.2.2
  bs58: ^1.0.2
  ed25519_hd_key: ^2.2.0
  eth_sig_util: ^0.0.9
  hex: ^0.2.0
  wallet_core_bindings: ^4.6.0
  wallet_core_bindings_native: ^4.6.0
  wallet_core_bindings_libs: ^4.6.0
  fixnum: ^1.1.0
  protobuf: ^6.0.0
  url_launcher: ^6.3.0
  external_app_launcher: ^4.0.0
  share_plus: ^10.1.4
  webview_flutter: ^4.4.4
  flutter_custom_tabs: ^2.1.0
  
  # JSON Serialization
  json_annotation: ^4.9.0
  
  # Internationalization
  intl: 0.19.0
  easy_localization: ^3.0.7+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^3.0.0
  build_runner: ^2.4.7
  json_serializable: ^6.7.1
  flutter_launcher_icons: ^0.13.1
  msix: ^3.16.13

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/locales/
    - assets/tsp_agent/default_agent.yml
    - assets/logo.png
    - assets/logo-512.png
    - assets/logo-1024.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
# For details regarding fonts from package dependencies,
# see https://flutter.dev/to/font-from-package

# ویندوز: 1.0.0 لینک WinRT را گاهی خراب می‌کند؛ 1.0.3+ به‌روزتر است.
dependency_overrides:
  flutter_local_notifications_windows: ^1.0.3

# بسته نصب MSIX — بعد از: flutter build windows --release
# اجرا: dart run msix:create   (با build_windows: false بیلد را جدا انجام دهید؛ اسکریپت build_windows_release.ps1)
msix_config:
  display_name: CoinCeeper
  publisher_display_name: CoinCeeper
  identity_name: com.coinceeper.adl.wallet
  # با line 19 هماهنگ: 1.0.38+38 → 1.0.38.0
  msix_version: 1.0.38.0
  logo_path: assets/images/logo.png
  architecture: x64
  capabilities: "internetClient, privateNetworkClientServer"
  # بیلد را build_windows_release.ps1 با vcvars انجام می‌دهد؛ دوبار کامپایل نشود
  build_windows: false
  output_path: build\windows\installer
  output_name: CoinCeeper_Installer
  execution_alias: coinceeper
  # امضا الزامی است؛ بدون آن App Installer خطای 0x800B010A می‌دهد.
  # ابتدا: scripts/msix/New-CoinCeeperMsixCert.ps1  سپس:
  # dart run msix:create --certificate-password <رمز_PFX>
  sign_msix: true
  install_certificate: true
  certificate_path: windows/msix_signing.pfx
  publisher: CN=CoinCeeper ADL, O=CoinCeeper, C=US

# Flutter Launcher Icons configuration
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/logo.png"
  background_color: "#FFFFFF"
  remove_alpha_ios: true
  min_sdk_android: 21 # android min sdk min:16, default 21
  web:
    generate: true
    image_path: "assets/images/logo.png"
    background_color: "#FFFFFF"
    theme_color: "#FFFFFF"
  windows:
    generate: true
    image_path: "assets/images/logo.png"
    icon_size: 48 # min:48, max:256, default: 48
  macos:
    generate: true
    image_path: "assets/images/logo.png"
